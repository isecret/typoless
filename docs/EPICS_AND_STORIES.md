# Typoless Epics And Stories

## 1. 文档目的

本文档将 [PRD.md](./PRD.md) 中已更新的产品需求拆分为可执行的 `Epics` 和 `Stories`，用于后续排期、任务分派和 issue 管理。

拆分原则：

- 以首版 `MVP` 为边界
- 优先保证主链路可用
- 每个 Story 必须具备明确交付结果
- 每个 Story 尽量可独立验收

## 2. Epic 总览

| Epic ID | 名称 | 目标 |
| --- | --- | --- |
| E1 | 应用骨架与菜单栏 | 建立 macOS 菜单栏应用基础能力 |
| E2 | 设置页与本地配置 | 提供首版配置与本地持久化能力 |
| E3 | 权限管理 | 完成麦克风与辅助功能权限检查与引导 |
| E4 | 全局快捷键与录音 | 打通“按住说话，松开处理”的音频采集链路 |
| E5 | ~~腾讯云 ASR 集成~~ | ~~已移除：收敛为本地 Whisper 唯一方案~~ |
| E6 | OpenAI 兼容 LLM 集成 | 完成文本纠错与轻度润色能力 |
| E7 | 文本注入与回退 | 将结果稳定写回当前焦点应用 |
| E8 | 状态机、错误处理与取消 | 保证主链路状态稳定、可取消、可失败回退 |
| E9 | ~~最近记录与诊断~~ | ~~已移除：不再保存常规最近记录，改为注入失败文本临时复制入口~~ |
| E10 | 集成验证与发布准备 | 完成端到端验收与项目交付准备 |
| E11 | 诊断日志与 ASR/LLM 对照 | 建立可排查延迟和文本质量的 Debug 日志能力 |
| E12 | 本地音频降噪 | 通过 RNNoise 降低噪声对识别率的影响 |
| E13 | SenseVoice 本地识别 | 将默认 ASR 链路切换为本地 SenseVoice 离线识别 |
| E14 | Prompt 与个人词典 | 优化 LLM 纠错边界，并引入术语词典提升专名稳定性 |
| E15 | 本地 ASR 运行时与模型资源 | 管理内置 `sherpa-onnx` 运行时、SenseVoice 模型与降噪资源 |
| E16 | SenseVoice 新链路集成验收 | 验证降噪、SenseVoice、LLM 和注入的完整闭环 |
| E17 | ASR 平台选择与模型外置 | 支持本地 SenseVoice 外置模型下载和多云 ASR 平台 |
| E18 | LLM 保守型结构化处理 | 在不扩写、不改原意前提下提升列表化、消息化和自我修正处理稳定性 |
| E19 | 长录音分段 ASR | 通过音频静音分段支持长录音，55s 分段路由至现有 ASR 平台 |

## 3. Epic 详情

## E1. 应用骨架与菜单栏

### 目标

建立首版 macOS 菜单栏应用骨架，提供最基础的应用入口和状态展示能力。

### Stories

#### S1.1 初始化 macOS 应用工程

作为开发者，我需要一个基于 `Swift + SwiftUI + AppKit` 的项目骨架，以便后续实现菜单栏应用。

验收标准：

- 工程可在 Xcode 中打开并运行
- 应用启动后可正常驻留于菜单栏
- 项目目录结构清晰，至少包含 UI、Domain、Providers、Platform、Persistence 分层入口
- 客户端整体架构采用 `MVVM + Service Layer`

#### S1.2 实现菜单栏图标与菜单

作为用户，我希望应用在菜单栏中有基础入口，以便访问核心功能。

验收标准：

- 菜单栏图标可见
- 菜单中包含 `打开设置`、`退出应用`
- 处理中状态下菜单中出现 `取消当前任务`
- 注入失败时菜单中显示失败文本预览，可点击复制

#### S1.3 实现状态驱动的菜单栏反馈

作为用户，我希望菜单栏图标能表达当前处理状态，以便知道应用正在做什么。

验收标准：

- 支持显示 `空闲`、`录音中`、`识别中`、`润色中`、`注入中`、`失败`
- 状态变化能随主链路正确刷新

依赖：

- 无

## E2. 设置页与本地配置

### 目标

提供首版必需的配置入口，并实现普通配置与密钥的本地持久化。

### Stories

#### S2.1 实现设置页基础框架

作为用户，我希望有一个统一的设置页配置应用，以便完成首次初始化。

验收标准：

- 应用可从菜单打开设置页
- 首次启动自动打开设置页
- 设置页包含 LLM、快捷键、权限、诊断模块占位

#### ~~S2.2 实现腾讯云 ASR 配置表单~~

> 已移除：ASR 方案已收敛为本地 Whisper，不再需要腾讯云配置表单。

#### S2.3 实现 OpenAI 兼容 LLM 配置表单

作为用户，我希望填写 LLM 配置，以便应用可以完成文本润色。

验收标准：

- 支持输入 `Base URL`、`API Key`、`Model`
- 支持在 `Base URL` 和 `API Key` 完整时从 OpenAI 兼容 `/models` 获取候选模型
- 当服务不支持 `/models` 或获取失败时，仍可手动输入 `Model`
- 配置值可保存并重新加载

#### S2.4 实现功能开关与快捷键配置项

作为用户，我希望设置全局快捷键，以便稳定触发录音。

验收标准：

- 支持设置并展示全局快捷键
- 配置值可保存并重新加载

#### S2.5 实现配置存储策略

作为开发者，我需要区分普通设置和敏感密钥的存储位置，以保证安全性和可维护性。

验收标准：

- LLM API Key 存储到 `~/.typoless/config.json`
- 普通设置存储到 `UserDefaults`
- 应用重启后可恢复配置
- 保存配置时做轻量格式校验，真正联网调用时再做严格失败

依赖：

- E1

## E3. 权限管理

### 目标

完成首版所需的麦克风权限与辅助功能权限引导和状态展示。

### Stories

#### S3.1 实现麦克风权限检测与引导

作为用户，我希望知道应用是否有录音权限，并能被引导去授权。

验收标准：

- 设置页可展示麦克风权限状态
- 权限缺失时提供明确引导
- 未授权时无法开始录音

#### S3.2 实现辅助功能权限检测与引导

作为用户，我希望知道应用是否有文本注入权限，并能被引导去授权。

验收标准：

- 设置页可展示辅助功能权限状态
- 权限缺失时提供明确引导
- 未授权时注入动作会失败并返回明确错误

#### S3.3 实现首次配置权限流程

作为用户，我希望首次打开设置页时能按顺序完成权限准备，而不是在主流程里被打断。

验收标准：

- 首次启动自动打开设置页
- 设置页中权限区展示引导顺序清晰
- 权限申请时机与设置页流程一致

依赖：

- E1
- E2

## E4. 全局快捷键与录音

### 目标

打通首版录音能力，支持全局快捷键按下切换模式触发录音。

### Stories

#### S4.1 实现全局快捷键注册

作为用户，我希望通过全局快捷键触发录音，而不必切换到应用窗口。

验收标准：

- 应用可注册单一全局快捷键
- 快捷键在常驻菜单栏状态下可生效
- 修改快捷键后立即生效

#### S4.2 实现按下切换录音模式

作为用户，我希望按一次快捷键开始录音、再按一次结束录音，以获得简洁直觉的交互。

验收标准：

- 按下快捷键开始录音
- 再次按下快捷键结束录音
- 处理中（`transcribing / polishing / injecting`）再次按键忽略
- 录音状态能反映到菜单栏

#### S4.3 ~~实现录音时长与超时控制~~（已由 E19 取代）

~~作为产品，我需要限制单次录音长度，以控制延迟和成本。~~

> **注意**：固定 60 秒录音上限已由 E19 长录音分段 ASR 方案取代。App 不再设置固定录音时长上限，改为通过 AudioSegmenter 自动分段（55s 强制切段 + 静音检测切段），分段提交现有 ASR 平台。

#### S4.6 实现短录音静默取消

作为用户，我希望误触或极短录音不会触发识别、润色和注入，避免产生奇怪文本或浪费处理资源。

验收标准：

- 录音时长低于 `500ms` 时静默取消
- 短录音不进入降噪、ASR、LLM 或文本注入
- 短录音不展示错误摘要
- 短录音不生成注入失败文本
- 诊断日志记录 `short_recording_cancelled`

#### S4.4 实现音频格式标准化

作为开发者，我需要统一录音输出格式，以便稳定提交给 ASR。

验收标准：

- 音频输出格式固定为 `PCM/WAV 16k mono`
- 主链路可复用标准化后的音频结果

#### S4.7 实现菜单栏麦克风选择

作为用户，我希望在菜单栏直接选择录音麦克风，以便在多个输入设备之间快速切换。

验收标准：

- 菜单栏一级菜单包含 `麦克风` 子菜单，不额外展示当前设备名称
- 子菜单包含 `系统默认($当前选择输入设备名)` 和当前所有可用麦克风
- 当前选择用勾选展示
- 点击设备后立即保存配置，但仅影响下一次录音
- 录音、识别、润色、注入期间禁用麦克风切换
- 已选设备不可用时自动切换到系统默认输入，不额外展示不可用提示项
- 音频输出仍固定为 `PCM/WAV 16k mono`

#### ~~S4.5 实现可配置录音触发方式~~

> 已废弃：需求变更为仅保留按下切换模式，不再支持双模式切换。见 S4.2。

依赖：

- E1
- E2
- E3

## ~~E5. 腾讯云 ASR 集成~~

> 已移除：ASR 方案已收敛为本地 Whisper 唯一方案，腾讯云 ASR 相关 Stories 不再适用。

## E6. OpenAI 兼容 LLM 集成

### 目标

接入用户自定义 OpenAI 兼容模型服务，实现固定边界的文本润色。

### Stories

#### S6.1 实现 OpenAI 兼容 LLM Provider

作为开发者，我需要一个标准化的 LLM 调用层，以便对接不同 OpenAI 兼容网关。

验收标准：

- 对齐 `OpenAI Chat Completions` 接口形态
- 支持 `Base URL`、`API Key`、`Model`
- 调用层独立于 UI
- 可返回统一的文本结果模型
- Provider 内部负责固定超时与取消控制

#### S6.2 实现固定润色 Prompt 策略

作为产品，我希望首版的 LLM 行为边界稳定可控。

验收标准：

- Prompt 明确限制为纠错、同音词/错别字修正、去赘词、轻度书面化、补中文标点
- Prompt 明确要求保留个人词典中的专有名词
- 不暴露用户自定义 Prompt 能力
- 不暴露高级模型参数

#### S6.3 基于 LLM 配置自动启用润色

作为用户，我希望只要 LLM 配置完整就自动启用润色，避免额外开关带来的歧义。

验收标准：

- 不再提供独立的 `启用 AI 润色` 开关
- `Base URL`、`API Key`、`Model` 三项完整时自动启用 LLM
- 三项全空或半填时允许保存，但运行时视为未配置完成

#### S6.4 实现 LLM 失败处理

作为用户，我希望 LLM 配置异常或请求失败时得到明确错误，而不是被自动注入未经润色的文本。

验收标准：

- LLM 配置不完整时直接报错，不执行注入
- LLM 超时、失败、空结果时直接报错，不回退 ASR 原文
- 仅文本注入失败时保留失败文本供菜单栏复制

依赖：

- E2

## E7. 文本注入与回退

### 目标

把最终结果稳定写入当前焦点应用，并在失败时提供最小可用回退。

### Stories

#### S7.1 实现文本注入能力

作为用户，我希望识别结果能直接进入当前输入框，而不是停留在应用内部。

验收标准：

- 默认通过剪贴板 + 粘贴快捷键注入文本
- 粘贴未生效时回退到焦点元素 `AX` 写值
- 适配常见场景：浏览器输入框、备忘录、聊天应用

#### S7.2 实现注入失败处理

作为用户，我希望注入失败时仍然能拿到结果文本。

验收标准：

- 注入失败时返回用户可理解错误
- 不自动覆盖剪贴板
- 失败文本保留在内存中，菜单栏显示截断预览，点击可复制到剪贴板
- 该失败文本仅在当前运行期有效，下一次成功注入后自动清空

#### S7.3 实现辅助功能权限校验接入注入链路

作为系统，我需要在注入前确认权限满足条件，以避免无意义失败。

验收标准：

- 注入前进行辅助功能权限检查
- 缺权限时终止注入并提示

依赖：

- E3
- E6

## E8. 状态机、错误处理与取消

### 目标

保证主链路稳定串行运行，并支持取消、错误分类和状态驱动 UI。

### Stories

#### S8.1 实现 Session Coordinator

作为开发者，我需要统一的主链路协调器管理状态和依赖，以避免流程散落各处。

验收标准：

- 串行控制 `idle -> recording -> transcribing -> polishing -> injecting -> done`
- 支持 `error` 和 `cancelled` 异常状态
- 同一时间只允许一个 session

#### S8.2 实现用户可理解的错误分类

作为用户，我希望看到简明错误，而不是底层异常信息。

验收标准：

- 覆盖麦克风权限、辅助功能权限、本地识别失败、LLM 凭证或模型、LLM 网络、注入失败、用户取消
- 错误可用于菜单栏和设置页摘要展示

#### S8.3 实现处理中取消能力

作为用户，我希望识别或润色卡住时可以主动终止当前任务。

验收标准：

- 识别中和润色中可从菜单栏取消
- 取消后请求停止或结果被丢弃
- 取消后不进入文本注入

#### S8.4 优化 HUD 失败反馈

作为用户，我希望 HUD 在失败时给出更清晰的短提示，而不是只看到通用"失败"，以便知道是权限、识别、润色还是写入出了问题。

验收标准：

- HUD 录音态保持现有声波、取消、确认入口
- HUD 处理态继续使用 `THINKING`，不拆分多阶段处理状态
- HUD 失败态支持 `缺少权限`、`资源缺失`、`没听清`、`识别失败`、`润色失败`、`写入失败` 等短文案
- HUD 不展示底层错误、ASR 原文、LLM 输出或长说明
- 短录音静默取消不显示失败态

#### S8.5 原型验证 HUD 注入失败复制入口

作为用户，我希望当文本写入失败时，可以在 HUD 中快速复制本次结果文本，避免需要再去菜单栏寻找恢复入口。

验收标准：

- 先在 HTML 原型中完成注入失败 HUD 形态验证
- 原型至少覆盖：普通失败、注入失败（带复制入口）、复制成功
- 注入失败态可表达 `写入失败` 与复制动作
- 点击复制后应有 `已复制` 反馈
- 原型确认前不进入 SwiftUI HUD 复制入口实现
- 复制入口仅用于文本注入失败，不用于 LLM 失败、ASR 失败、权限失败、取消或短录音
- 复制动作必须由用户主动触发，不得自动写入剪贴板

依赖：

- E4
- E6
- E7

## ~~E9. 最近记录与诊断~~

> 已移除：不再保存常规最近记录。注入失败文本通过内存态临时复制入口恢复，相关逻辑已内嵌到 E7（注入失败处理）和 E8（错误摘要展示）。

### ~~Stories~~

#### ~~S9.1 实现最近记录存储~~

> 已移除：不再持久化保存文本记录。

#### ~~S9.2 实现最近记录展示与复制~~

> 已移除：不再展示历史记录列表。

#### ~~S9.3 实现最近记录清空~~

> 已移除：不再维护历史记录。

#### S9.4 实现最近一次错误摘要

作为用户，我希望在设置页看到最近一次失败的摘要，便于排查配置问题。

验收标准：

- 设置页展示最近一次错误摘要
- 摘要内容使用用户可理解语言

依赖：

- E2
- E8

## E10. 集成验证与发布准备

### 目标

完成首版 MVP 的联调、验收和交付准备。

### Stories

#### S10.1 完成主链路端到端联调

作为团队，我需要确认从录音到注入的完整链路可用。

验收标准：

- 在真实配置下完成录音 -> ASR -> LLM -> 注入
- 常见输入场景验证通过

#### S10.2 完成失败场景验证

作为团队，我需要确保异常路径可控，避免交付后出现不可恢复问题。

验收标准：

- 验证权限缺失场景
- 验证本地识别和 LLM 配置错误场景
- 验证 LLM 回退逻辑
- 验证注入失败后可从菜单栏复制失败文本

#### S10.3 完成交付文档与开发说明

作为团队，我需要最小可维护文档，支持后续开发和验收。

验收标准：

- README 与 PRD 保持一致
- 补充实现说明或开发说明文档
- 明确已支持和未支持范围

#### S10.4 实现 Sparkle 应用内更新与 tag 驱动版本同步

作为用户，我希望应用能在 GitHub Releases 发布新版本后直接在应用内收到并安装更新，并在关于窗口主动检查更新。

验收标准：

- 关于窗口包含 `自动检查更新` 开关和 `检查更新` 按钮
- 启动后可按开关状态自动检查 `updates/appcast.xml` 中的最新稳定版
- 发现更新后可在应用内完成下载与安装，不需要跳转浏览器
- 菜单栏中不增加更新入口
- 发布工作流从 `v*` tag 自动同步应用版本号，不再依赖手动更新工程版本

依赖：

- E1
- E2
- E3
- E4
- E6
- E7
- E8

## E11. 诊断日志与 ASR/LLM 对照

### 目标

建立主链路诊断能力，用于定位“慢在哪”和“错在哪”，同时不破坏 Release 隐私边界。

### Stories

#### S11.1 实现主链路耗时 Debug 日志

作为开发者，我需要看到每次会话各阶段耗时，以便定位端到端延迟来源。

验收标准：

- 每次 session 生成唯一 `session_id`
- 日志记录 recording、ASR、LLM、injection、total 耗时
- 日志记录目标应用 bundle id、结果来源、错误分类
- 日志不写入用户可见历史记录

#### S11.2 实现 ASR/LLM 对照日志

作为开发者，我需要对照 ASR 原文和 LLM 输出，以便判断错别字来自识别还是润色。

验收标准：

- Debug 构建可输出 ASR 原文和 LLM 输出
- Release 构建仅输出长度、hash、来源等脱敏摘要
- LLM 回退时记录回退原因
- 不记录 API Key、原始音频或完整底层响应体

## E12. 本地音频降噪

### 目标

在 ASR 前加入本地降噪，降低环境噪声对中文短语音识别的影响。

### Stories

#### S12.1 集成 RNNoise 降噪库

作为开发者，我需要将开源 RNNoise 集成到 macOS 客户端，以便在本地完成语音降噪。

验收标准：

- RNNoise 作为本地库参与构建
- 不依赖云端服务
- 许可证与第三方来源在文档中说明

#### S12.2 实现录音后降噪处理链路

作为用户，我希望录音在识别前自动降噪，以减少噪声导致的错别字。

验收标准：

- 录音结束后、ASR 前执行降噪
- 降噪输出仍为 ASR 可消费的 16k mono WAV
- 降噪失败返回明确错误并停止本次处理
- 不保存降噪前后的音频历史

## E13. SenseVoice 本地识别

### 目标

将默认 ASR 切换为本地 `SenseVoiceSmall-onnx` 离线识别，通过内置 `sherpa-onnx` 运行时加载固定 ONNX 模型。

### Stories

#### S13.1 接入 `sherpa-onnx` 运行时与固定模型组合

作为开发者，我需要在项目中集成 `sherpa-onnx` 运行时与 SenseVoice 模型加载能力，以便支持本地离线 ASR。

验收标准：

- 支持 macOS arm64 运行本地 runtime
- 使用固定模型组合 `SenseVoiceSmall-onnx`
- runtime 可定位本地模型目录并完成加载
- 单次 WAV 请求可返回转写文本
- 不暴露流式 partial 结果

#### S13.2 实现 SenseVoiceASRProvider 与 SenseVoiceRuntimeManager

作为系统，我需要 Swift 端 Provider 管理本地 runtime 生命周期与识别请求。

验收标准：

- SenseVoiceASRProvider 接受 WAV 数据并返回 TranscriptResult
- 首次录音时惰性预热 runtime 成功
- ASR 超时（按分段时长动态计算：`min(90s, max(15s, segmentDurationSeconds * 1.3 + 10s))`）、取消、runtime 异常可正常清理和恢复
- 错误映射到统一 ASR 错误模型
- 通过本地 `sherpa-onnx` C API 调用

#### S13.3 将 SenseVoice 设为默认 ASR 链路并移除旧默认实现

作为用户，我希望默认使用 SenseVoice 本地离线识别。

验收标准：

- 默认主链路使用 SenseVoiceASRProvider
- 资源缺失时不回退到旧 ASR，直接阻止录音并提示配置错误
- 旧 sherpa-onnx 和 Whisper 默认路径分支已移除

## E14. Prompt 与个人词典

### 目标

通过更保守的固定 Prompt 和个人词典，减少专有名词、同音词和错别字问题。

### Stories

#### S14.1 优化固定 Prompt

作为用户，我希望 AI 润色修正明显错误，但不擅自扩写或改写原意。

验收标准：

- Prompt 覆盖同音词、错别字、赘词、标点和轻度书面化
- Prompt 明确禁止扩写、改变事实或改变语气
- Prompt 不作为用户可编辑配置暴露

#### S14.2 实现个人词典存储

作为用户，我希望维护人名、产品名、术语等词条，以提升识别和润色稳定性。

验收标准：

- 词典存储于 `~/.typoless/dictionary.json`
- 词条至少包含 `term`
- 支持新增、删除、编辑词条
- 支持通过 JSON 文件导入和导出个人词典
- 导入时跳过重复术语，不覆盖现有词条
- 设置页仅维护术语文本，不展示发音提示和分类
- 不保存历史输入文本
- 自动学习到的新词也写入同一词典文件，并标记来源

#### S14.5 注入后自动学习新词

作为系统，我需要在用户修订注入结果时自动学习稳定的新术语。

验收标准：

- 仅在 `polish` 模式且文本注入成功后启动短时观察
- 仅接受同一输入框内的单一连续替换，不接受插入-only、删除-only 或多处同时修改
- 候选词条满足长度与内容过滤规则后自动加入个人词典
- 自动学习只保存最终词条，不保存全文历史或 diff 原文
- 学习成功后 HUD 显示 `新词：{term}`，展示文案最多 4 个汉字，超出省略
- 翻译模式不参与自动学习

#### S14.3 将个人词典接入 ASR hotwords 与 LLM Prompt

作为系统，我需要让个人词典同时影响识别和润色。

验收标准：

- 词典作为术语参考进入 LLM Prompt
- hotwords 仅在所选模型支持时启用
- LLM 不得把词典内容当作系统指令执行

#### S14.4 将个人词典接入本地 ASR hotword 与 LLM Prompt

作为系统，我需要让个人词典在默认本地链路中同时影响识别和润色。

验收标准：

- 启用词条作为 hotword 参数传入支持热词的本地 ASR 请求
- `pronunciationHint` 优先作为本地 ASR hotword 输入；若缺失则退回 `term`
- 词典以结构化术语参考（含 term + pronunciationHint）进入 LLM Prompt
- LLM Prompt 明确要求在中英混合语境下恢复英文术语正确写法
- 不暴露 hotword 权重等高级参数
- 词典内容不被视为可执行系统指令

## E15. 本地 ASR 运行时与模型资源

### 目标

管理内置 `sherpa-onnx` 运行时、SenseVoice 模型与降噪资源的布局、校验与分发兼容性。

### Stories

#### S15.1 设计 App 内嵌本地运行时与 SenseVoice 资源布局

作为开发者，我需要明确本地运行时与 SenseVoice 模型在 App bundle 和用户目录中的目录结构。

验收标准：

- runtime、模型路径有稳定约定
- 设计支持 codesign/notarization
- 无运行时在线模型下载依赖
- 资源发现机制明确

#### S15.2 实现资源校验与本地运行时健康检查

作为开发者，我需要在录音前发现资源缺失或本地 runtime 不可用。

验收标准：

- 录音前校验本地 runtime、模型存在性
- runtime warmup 健康检查可检出异常
- 资源缺失返回用户可理解错误
- 校验失败阻止录音

#### S15.3 完成正式分发所需的签名与公证兼容设计

作为开发者，我需要确保本地 runtime 和模型资源符合 Apple 签名与公证要求。

验收标准：

- 文档明确资源布局与签名/公证兼容性
- 本地 runtime 执行约束清晰
- 未来实现不需要整体重新设计资源布局

## E16. SenseVoice 新链路集成验收

### 目标

验证降噪、SenseVoice 离线 ASR、LLM 润色、个人词典和文本注入能形成稳定闭环。

### Stories

#### S16.1 完成 SenseVoice 默认链路端到端验证

作为团队，我需要确认默认新链路可完成一次真实输入。

验收标准：

- 完成录音 -> 降噪 -> SenseVoice -> LLM -> 注入
- 浏览器输入框可完成注入
- 备忘录/常见编辑器可完成注入
- 低于 500ms 的短录音静默取消
- 长录音通过 AudioSegmenter 自动分段，分段 ASR 结果按序拼接
- 首次调用惰性预热 runtime 成功并完成主链路
- LLM 失败时直接报错且不注入文本

#### S16.2 验证 SenseVoice 失败处理与 LLM 失败行为

作为团队，我需要确认异常路径可控且明确。

验收标准：

- 资源异常不会进入不可理解失败
- ASR 超时、取消后可再次正常开始识别
- runtime 异常或资源恢复后可继续
- LLM 失败直接报错且不注入文本
- HUD 失败态展示分类短文案而非通用"失败"
- HTML 原型（`docs/hud-failure-prototype.html`）通过产品确认后方可实现 SwiftUI 注入失败复制入口

#### S16.3 验证内置本地 runtime 的签名分发可运行性

作为团队，我需要确认签名构建环境下本地 runtime 可正常运行。

验收标准：

- 签名构建中本地 runtime 可启动并完成一次识别
- runtime 和模型路径稳定可定位
- 形成可复用的 release 检查清单

## 4. 推荐实现顺序

建议按以下顺序推进：

1. `E1 应用骨架与菜单栏`
2. `E2 设置页与本地配置`
3. `E3 权限管理`
4. `E4 全局快捷键与录音`
5. `E6 OpenAI 兼容 LLM 集成`
6. `E7 文本注入与回退`
7. `E8 状态机、错误处理与取消`
8. `~~E9 最近记录与诊断~~`（已移除）
9. `E10 集成验证与发布准备`
10. `E11 诊断日志与 ASR/LLM 对照`
11. `E12 本地音频降噪`
12. `E14 Prompt 与个人词典`
13. `E15 资源准备与校验`
14. `E13 SenseVoice 本地识别`
15. `E15 本地 ASR 运行时与模型资源`
16. `E16 SenseVoice 新链路集成验收`
17. `E17 ASR 平台选择与模型外置`
18. `E18 LLM 保守型结构化处理`
19. `E19 长录音分段 ASR`

## E17. ASR 平台选择与模型外置

### 目标

将本地 SenseVoice 模型改为用户目录外置下载，并新增腾讯云、阿里云、火山引擎、科大讯飞、小米 MiMo、小米 MiMo（Token Plan）作为可选云 ASR 平台。

### Stories

#### S17.1 ASR 配置模型与平台切换

作为用户，我希望在设置页选择 ASR 平台（本地 SenseVoice 或六个云端入口），以便根据需求选择合适的识别方式。

验收标准：

- `AppConfig` 新增 `ASRPlatform`、`ASRConfig`、`LocalASRConfig`、`TencentASRConfig`、`AliyunASRConfig`、`VolcengineASRConfig`、`XunfeiASRConfig`、`XiaomiMiMoASRConfig` 模型
- `ConfigStore` 提供 `asrConfig` 读写和 `isASRReady` 判断
- 平台切换通过 `selectedPlatform` 字段控制
- 配置不完整时 `isASRReady` 返回 `false` 并携带原因描述
- 云端平台新增持久化验证状态（unvalidated / validating / verified / failed）与最近失败摘要
- 云端平台仅在字段完整且真实请求验证成功后才视为 ready

#### S17.2 本地模型下载管理

作为用户，我希望在设置页下载本地 SenseVoice 模型，不再依赖 App 打包模型。

验收标准：

- `ModelDownloadManager` 管理 `~/.typoless/models/sensevoice-small-onnx/` 下模型的下载、验证和删除
- 默认使用 Hugging Face 上 sherpa-onnx 维护的模型文件下载
- 下载进度通过 `AsyncStream` 实时报告
- 支持配置镜像源
- 模型状态跟踪（notDownloaded / downloading / ready / failed）
- 设置页以单一状态区承载状态与操作：未下载显示下载，下载中显示进度与取消，已就绪仅显示完成态
- 设置页不向用户暴露模型路径、模型版本、重新下载或删除入口
- `ResourceValidator` 支持从用户目录校验模型

#### S17.3 多云 ASR 集成

作为用户，我希望使用云端 ASR，无需本地模型即可进行语音识别。

验收标准：

- `TencentSentenceASRProvider` 实现 `ASRProvider` 协议
- `AliyunSentenceASRProvider`、`VolcengineSentenceASRProvider`、`XunfeiSentenceASRProvider`、`XiaomiMiMoASRProvider` 实现 `ASRProvider` 协议
- 腾讯云使用 TC3-HMAC-SHA256；阿里云使用 `CreateToken + RESTful ASR`；火山引擎使用文件识别接口；科大讯飞使用 WebSocket 语音听写；小米 MiMo 与小米 MiMo（Token Plan）使用 OpenAI Chat Completions 兼容非流式接口，二者仅 Base URL 不同
- 各云平台超时按分段时长动态计算：`min(90s, max(15s, segmentDurationSeconds * 1.3 + 10s))`
- 配置项最小化：只暴露调用所需凭据
- 错误映射完整：配置不全、鉴权失败、网络错误、空响应、响应无效
- 设置页保存云端凭据后自动触发真实验证，并展示验证中 / 已就绪 / 验证失败状态
- 修改任一云端凭据字段后，原验证结果立即失效

#### S17.4 录音链路适配多 ASR 平台

作为用户，我希望录音后自动使用已选定的 ASR 平台进行识别。

验收标准：

- `SessionCoordinator` 录音前检查 `isASRReady`，未就绪时阻止录音
- 本地平台走 `SenseVoiceRuntimeManager` 预热 + `SenseVoiceASRProvider`
- 云平台通过 `ASRProviderFactory` 分发到腾讯云、阿里云、火山引擎、科大讯飞、小米 MiMo、小米 MiMo（Token Plan）对应 Provider
- 不做平台间自动回退

## E18. LLM 保守型结构化处理

### 目标

在保持 OpenAI Chat Completions 协议、固定 Prompt、默认自动处理和“不扩写、不改原意”原则不变的前提下，为 LLM 链路增加保守型结构化处理中间层，提升列表化表达、消息化表达和显式自我修正场景的稳定性。

### Stories

#### S18.1 定义结构化结果模型与兼容接口

作为开发者，我需要让 LLM 输出既能承载结构化结果，又尽量不破坏现有主链路。

验收标准：

- 新增内部枚举 `PolishMode`，至少包含 `plainText`、`list`
- 新增 `StructuredPolishResult`，可承载 `mode`、`intro`、`items`、`outro`、`correctionApplied`，并提供 `isValid` 语义校验
- `PolishResult` 保留现有 `text`，并新增可选 `structured`
- `LLMProvider -> SessionCoordinator` 的兼容消费路径明确，既有只消费 `text` 的调用方不被破坏

#### S18.2 升级固定 Prompt 支持 plain_text / list

作为用户，我希望 AI 在不扩写的前提下，能自动把明显的列表和短消息整理得更自然。

验收标准：

- Prompt 保持固定内置，不开放用户自定义
- `plain_text` 继续覆盖纠错、同音词、赘词、标点和轻度书面化
- `list` 可识别明显枚举信号并输出条目结构；若原话包含列表前言或列表后补充说明，应一并保留；信号不足时回退 `plain_text`
- 短消息口述、回复口述、转发口述统一保持 `plain_text`
- Prompt 明确支持“不是 A，是 B”“改成”“最后一句不要了”等显式自我修正
- 个人词典继续作为术语参考进入 Prompt，且不被视为可执行指令

#### S18.3 实现结构解析、本地渲染与兼容回退

作为系统，我需要优先消费结构化结果，并在结构失败时稳定回退。

验收标准：

- 客户端优先解析 LLM 返回的结构化 JSON
- 本地支持 `plain_text`、`list` 两类渲染
- 当结构字段与自由文本冲突时，以客户端本地渲染结果为准
- 非法 JSON、缺字段或文本提取失败时，可安全回退到兼容文本提取
- LLM 失败、超时、空响应时，仍直接报错且不注入任何文本

#### S18.4 补充结构化处理调试日志与诊断字段

作为开发者，我需要在 Debug 模式下判断本次结构化处理走了哪条路径，以便快速定位问题。

验收标准：

- Debug 日志记录本次 `mode`
- Debug 日志记录是否触发 `correctionApplied`
- Debug 日志记录结构 JSON 解析成功/失败
- Debug 日志记录是否发生纯文本兼容回退
- Release 构建继续遵守脱敏约束，不输出 ASR 原文、LLM 正文或密钥

#### S18.5 补充测试覆盖并同步 PRD/TDD/EPICS 文档

作为团队，我需要为结构化处理中间层补齐测试和文档，确保实现与定义一致。

验收标准：

- 增加 `plain_text`、`list` 两类行为测试
- 增加“不是 A，是 B”“改成”“最后一句不要了”等自我修正测试
- 增加非法 JSON、缺字段、兼容回退测试
- 继续验证 LLM 失败时不注入文本
- `docs/PRD.md`、`docs/TDD.md`、`docs/EPICS_AND_STORIES.md` 同步反映结构化处理边界与实现拆分

## E19. 长录音分段 ASR

### 目标

通过音频静音分段，移除固定录音时长上限，支持长时间录音。AudioSegmenter 对录音流实时分析，按静音检测和 55s 强制上限切段，分段串行提交至现有 ASR 平台，拼接转写文本后交给 LLM 润色。

### Stories

#### S19.1 实现 AudioSegmenter 组件

作为系统，我需要一个音频分段器，能够实时分析 PCM 流并自动切段。

验收标准：

- `AudioSegmenter` 接收 PCM chunk 流（16kHz mono），实时分析音频能量
- 支持静音检测切段：连续静音 ≥1.6s 且已录 ≥15s 时切段
- 支持 55s 强制切段：单段达到 55s 时无条件切段
- 切段后保留 300ms 尾部静音避免截断语音
- 输出 `SealedSegment` 包含 `segmentIndex`、PCM 数据、时长、切段原因（静音/强制）
- 最终调用 `finalize()` 输出最后一段

#### S19.2 SessionCoordinator 分段 ASR 编排

作为系统，我需要 SessionCoordinator 管理分段 ASR 队列，实现录音与 ASR 并行。

验收标准：

- 录音期间，已 sealed 的分段立即提交降噪和 ASR，不等待录音结束
- 分段 ASR 严格串行执行，避免 sidecar/云服务并发问题
- 录音结束后识别最后一段
- 所有分段 ASR 完成后，按 `segmentIndex` 顺序拼接转写文本
- 拼接后文本超过 8000 字符时，返回 `transcriptTooLong` 错误
- 任一有效分段 ASR 失败时，整次流程失败，不注入部分文本
- 取消 session 时，同时取消录音和未完成的 ASR 任务

#### S19.3 ASR Provider 动态超时

作为系统，我需要所有 ASR Provider 支持按分段时长动态计算超时。

验收标准：

- 超时公式：`min(90s, max(15s, segmentDurationSeconds * 1.3 + 10s))`
- SenseVoiceASRProvider、TencentSentenceASRProvider 及其他云 Provider 统一适用
- Provider 接口支持传入分段时长参数

#### S19.4 LLM Prompt 分段转写上下文

作为系统，我需要 LLM Prompt 说明输入来自连续分段转写。

验收标准：

- 当输入来自多段分段转写时，Prompt 明确说明：输入来自同一次语音输入的连续分段转写
- Prompt 指示 LLM 按原始顺序理解为一段连续表达
- Prompt 允许合并因分段造成的断句，但禁止扩写、改写原意或补充事实

#### S19.5 分段诊断日志

作为开发者，我需要 session 级和 segment 级诊断字段，以便排查分段 ASR 问题。

验收标准：

- Session 级字段：`segment_count`、`queued_segment_count`、`max_pending_segments`、`total_recording_ms`、`joined_transcript_chars`
- Segment 级字段：`segment_index`、`segment_duration_ms`、`voiced_detected`、`forced_cut`、`silence_cut`、`asr_timeout_ms`、`asr_elapsed_ms`、`asr_result_chars`、`failure_reason`
- Debug 构建可输出完整字段；Release 构建遵守脱敏约束

#### S19.6 分段 ASR 测试覆盖

作为团队，我需要为分段 ASR 补齐测试覆盖。

验收标准：

- `AudioSegmenter` 单元测试：静音切段、强制切段、300ms 尾部保留、短录音不切段
- `SessionCoordinator` 分段编排测试：串行 ASR、结果拼接、失败中止、取消行为
- 动态超时公式验证
- 拼接文本超过 8000 字符的 `transcriptTooLong` 测试
- 分段诊断日志输出验证

## 5. MVP 完成定义

以下条件全部满足，视为首版 MVP 完成：

- 用户可完成首次配置和权限准备
- 用户可通过全局快捷键完成一次中文语音输入（支持长录音自动分段）
- RNNoise 可完成本地降噪处理
- SenseVoice 可返回有效转写
- OpenAI 兼容 LLM 可完成固定边界内的文本润色
- LLM 配置不完整或请求失败时不会注入任何文本
- 最终文本可注入常见 macOS 应用输入区域
- 关键错误可被用户理解和处理
- 注入失败时文本可通过菜单栏临时复制入口恢复
- 技术设计与实现约束已沉淀到 [TDD.md](./TDD.md)
- Debug 日志可用于分析各阶段耗时与 ASR/LLM 文本差异
- 个人词典可影响 ASR hotwords 和 LLM 术语参考

## 6. 参考

- 产品需求文档：[PRD.md](./PRD.md)
- 技术设计文档：[TDD.md](./TDD.md)
